spring:
  application:
    name: cloud-web-api
  datasource:
    url: jdbc:postgresql://localhost:5432/cloud_db
    driverClassName: org.postgresql.Driver
    username: rasm
    password: vaika
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

server:
  port: 8083

jwt:
  secret: your_secret_key_change_this_in_production_with_a_long_random_string_at_least_256_bits
  expiration: 86400000 # 24 hours in milliseconds

firebase:
  service-account: ../cloud-web-mobile-firebase-adminsdk-fbsvc-25a331b444.json

logging:
  level:
    root: INFO
    com.cloudweb: DEBUG
